Js有下面几个核心概念：
1. js是单线程的，当遇到异步任务时候会跳过，并把异步事件添加给浏览器的事件表，当达到触发异步事件的时机后，浏览器将该异步事件放进 Js的事件队列里面，所有事件遵循先进先出的原则，js执行的单元是事件。

2. js刷新界面的事机是执行完一个事件后，注意是完成后，而不是在执行事件的过程中刷新界面UI，事件所对dom的操作都会在执行完一个事件后刷新出来。而刷新完界面后，再取下一个事件。



异步事件包含哪些：
1. js事件，比如onclick，onload等
2. 定时器
3. ajax
